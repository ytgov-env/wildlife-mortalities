@attribute [Route(Constants.Routes.CreateReportPage)]
@attribute [Route(Constants.Routes.EditDraftReportPage)]
@using WildlifeMortalities.App.Extensions
@using WildlifeMortalities.Data.Entities.Users;
@using WildlifeMortalities.Shared.Extensions;

@attribute [Authorize(Roles = PermissionConstants.Reports.CreateHarvest)]

<MortalityReportComponent DraftId="@DraftId" ReportId="@ReportId" HumanReadablePersonId="@HumanReadablePersonId" />

@code {

    [Parameter]
    public int? DraftId { get; set; }

    [Parameter]
    public int? ReportId { get; set; }

    [Parameter]
    public string HumanReadablePersonId { get; set; } = null!;
}