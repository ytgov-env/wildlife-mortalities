@using WildlifeMortalities.Shared.Extensions;
@using static WildlifeMortalities.Data.Entities.Mortalities.CaribouMortality;

@*@if(ViewModel.Id != null)
{
    <MudSelect T="CaribouHerd?" Value="@ViewModel.LegalHerd" Label="Herd" For="() => ViewModel.LegalHerd" Disabled="true">
        @foreach (var item in Enum.GetValues<CaribouHerd>())
        {
            <MudSelectItem T="CaribouHerd?" Value="@item">@item.GetDisplayName()</MudSelectItem>
        }
    </MudSelect>
}*@

@code {
    [Parameter]
    public CaribouMortalityViewModel ViewModel { get; set; } = default!;
}
